image:
  prefix: pulse
  tag: dev
  pullPolicy: IfNotPresent

api:
  replicas: 2
  port: 8080
  resources:
    requests: { cpu: 50m, memory: 64Mi }
    limits:   { memory: 128Mi }

scheduler:
  resources:
    requests: { cpu: 25m, memory: 32Mi }
    limits:   { memory: 64Mi }

worker:
  replicas: 2
  region: local
  resources:
    requests: { cpu: 50m, memory: 64Mi }
    limits:   { memory: 128Mi }

metricsPort: 9090

# In-cluster dependencies for dev. Disabled in stage/prod (RDS + ElastiCache).
postgres:
  enabled: false
  storage: 1Gi
redis:
  enabled: false

# When postgres.enabled=false, supply the external URL via secrets.
secrets:
  databaseUrl: ""
  redisUrl: ""
  jwtSecret: ""

site:
  replicas: 1

web:
  replicas: 2
  resources:
    requests: { cpu: 10m, memory: 24Mi }
    limits:   { memory: 64Mi }

status:
  replicas: 2
  resources:
    requests: { cpu: 10m, memory: 24Mi }
    limits:   { memory: 64Mi }

# Ingress: same origin for dashboard + API, separate host for status pages.
ingress:
  enabled: false
  className: nginx
  appHost: pulse.localtest.me
  statusHost: status.localtest.me

# Reading credentials from AWS Secrets Manager. Off by default: local runs use
# a plain Secret rendered from the values below.
externalSecrets:
  enabled: false
  region: ca-central-1
  namespace: external-secrets
  serviceAccount: external-secrets
  databaseSecret: ""
  extra: {}
